<strong>Create Comment</strong>

<%= form_for [@post, @comment], html: {id: :create_comment} do |f| %>
<p><%= f.text_area :content %></p>
<%= f.hidden_field :user_id, value: current_user.id %>
<%= f.submit %>

<% end %>